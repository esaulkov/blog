%nav{class: "navbar navbar-default"}
  .container-fluid
    %div
      - if user_signed_in?
        = render partial: "layouts/user"
      - else
        = render partial: "layouts/guest"
           
